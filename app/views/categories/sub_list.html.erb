<% meta title: @sub_category.label,
   description: "IDeal #{@sub_category.label} Discounts and Info." %>

<h1>IDeal <strong><%= @sub_category.label %></strong> Discounts and Info.</h1>

<% if @children.present? %>
  <div class="row">
    <div class="col-md-6">
      <h2>Subcategories</h2>
      <ul class="list">
      <% @children.each do |sub_cat| %>
        <li class="list-item"><%= link_to "#{sub_cat.label}", businesses_for_category_and_subcategory_path(@category, sub_cat) %></li>
      <% end %>
    </div>
    <div class="col-md-6">
      <h2>States</h2>
      <ul class="list">
      <% @states.each do |state| %>
        <li class="list-item"><%= link_to "#{state.state}", state_category_sub_category_path(state.state, @category, @sub_category) %></li>
      <% end %>
      </ul>
    </div>
  </div>
<% else %>
  <h2>Choose a state</h2>
  <ul class="list">
  <% @states.each do |state| %>
    <li class="list-item"><%= link_to "#{state.state}", state_category_sub_category_path(state.state, @category, @sub_category) %></li>
  <% end %>
  </ul>
<% end %>
