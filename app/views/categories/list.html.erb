<h1><%= @category.label %></h1>

<div class="row">
  <div class="col-md-6">
    <h2>Sub-Business Categories</h2>
    <ul>
    </ul>
  </div>
  <div class="col-md-6">
    <h2>States</h2>
    <ul class="list">
    <% @states.each do |state| %>
      <li class="list-item"><%= link_to "#{state.state} (#{state.count})", state_path(state.state) %></li>
    <% end %>
    </ul>
  </div>
</div>