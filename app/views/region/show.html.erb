<% meta title: "#{@region.name}",
   description: "IDeal discounts and info near #{@region.name}." %>

<h1>IDeal discounts and info near <%= @region.name %>.</h1>
<h3>Categories</h3>
<% @categories.each do |category| %>
  <span><%= link_to "#{category.label}", region_category_path(@region, category) %></span>
<% end %>
<%= render "businesses/businesses_list" %>
